# Security review — M1-3

STATUS: CLEAR

Iteration 2 re-review (2026-08-22). Iteration 1 was STATUS: FINDINGS; the
two fix-now findings (F1 MEDIUM, F4 LOW) are verified fixed below. F2, F3,
F5, F6 and the test-strictness notes are deferred and tracked as M1-5 in
FEATURES.md (verified present, lines 309-344) — not silently dropped.

## Fix verification
- F1 (MEDIUM, coverage exclude) — FIXED. vitest.config.mts:63-71 no longer
  excludes src/lib/addressValidation.ts; an explicit comment records why the
  subprocess justification does not apply to it. tests/test9-address-validation.test.ts
  covers region reject/accept (KE/ET/SO), partial-update semantics via an
  exact toEqual on the output object, non-boolean isDefault rejection, and
  the userId-injection case with real assertions on the output
  (not.toHaveProperty("userId") + Object.keys check) on both full and
  partial bodies. Every "data" branch is guarded by a prior
  expect("error" in result).toBe(false), so no assertion can no-op.
- F4 (LOW, single-default invariant) — FIXED at the DB level.
  prisma/migrations/20260822120000_address_one_default_per_user/migration.sql:15
  is a true WHERE-filtered partial unique index on ("userId") WHERE
  "isDefault" = true; isDefault=false rows remain unconstrained. No
  conflicting @@unique([userId]) added to schema.prisma (426-458, documented).
  P2002 handling in src/app/api/addresses/route.ts:78-84 and
  src/app/api/addresses/[id]/route.ts:95-101 matches the specific Prisma
  error class and code, returns a structured 409 JSON body, and rethrows
  everything else — not a catch-all. Address has no other unique constraint,
  so P2002 here is unambiguous. tests/test8-profile-addresses.test.ts:403-475
  forces genuine transaction overlap (holder $transaction holds an
  uncommitted isDefault=true on C while a real HTTP PATCH sets B), asserts
  409 + error body, and verifies exactly one default remains via a direct
  db.address.findMany query. The 409 assertion is impossible to satisfy
  without the index, so the test is non-trivial by construction.

## Checked (full pass against current state)
- PCI scope: no card data, payment token, or PaymentMethod access in any
  M1-3 file. Checked, no finding.
- Payment idempotency: no webhook or payment path in scope. N/A.
- Auth: all five handlers (PATCH /api/profile; GET+POST /api/addresses;
  GET+PATCH+DELETE /api/addresses/[id]) independently call
  auth.api.getSession() and 401 on absence. No hand-rolled credential
  field, custom JWT, or custom session table (AHD8 respected). No admin
  route in scope. Presence-check middleware: the M1-1 page-level gate at
  src/app/profile/page.tsx:14-21 is intact and un-weakened; the new API
  routes are outside middleware's /profile/:path* matcher and each does
  its own server-side session validation. Checked, no finding.
- Secrets: no literal values, no process.env reads, no console.* in any
  M1-3 src file (grepped src/app/{profile,api/profile,api/addresses}).
  No token or PII logging. Test credentials are disposable fixtures.
  Checked, no finding.
- Admin audit log: no admin mutation in scope — the actor is provably the
  subject (session-derived userId). Correctly absent. Checked, no finding.
- Input validation: server-side on every route; region enum-checked with
  no coercion; the create/update body is allowlisted to 7 keys so a
  client userId cannot survive (now proven by unit test, not just
  integration). Hand-rolled rather than Zod and unbounded string lengths
  remain deferred as M1-5 F2/F3. Checked, no blocking finding.
- Data residency: no region routing introduced; Address.region is a
  shipping label, consistent with the active eu-west-2 decision. U14
  follow-up tracked as M1-5 F6. Checked, no finding.

## Non-blocking observations (recommend folding into M1-5)
src/app/api/profile/route.ts:65-68 — generic catch returns 400 for any
  update failure. User.phone is @unique globally (schema.prisma:404), so
  400-vs-200 is a phone-number enumeration oracle for an authenticated
  user, and a genuine DB error is reported as a client 400. Unchanged
  since iteration 1; not blocking. — LOW
tests/test8-profile-addresses.test.ts:447 — the 1000 ms transaction hold
  is a timing assumption; if the HTTP round-trip exceeds it, the route
  self-heals and the test FAILS rather than falsely passing. Failing-
  direction flake only, acceptable. — INFO
