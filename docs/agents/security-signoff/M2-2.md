# Security review — M2-2

STATUS: CLEAR

Iteration 2 (post-fix). F1 (MEDIUM) verified CLOSED by code read. Remaining
F2/F3/F4 are LOW/informational and non-blocking; deferred to subsequent items.

Iteration 2 reads (full files, not summaries): src/lib/searchParams.ts,
src/lib/productService.ts:195-424, tests/test13-product-search.test.ts,
src/app/products/page.tsx. Iteration 1 reads carried forward:
src/components/SearchBar.tsx, src/components/FilterPanel.tsx, vitest.config.mts,
src/lib/region.ts, src/middleware.ts, src/lib/seed.ts, prisma migration, FEATURES.md M2-2.

## F1 (MEDIUM) — CLOSED
Bounds enforced in parseSearchState: MAX_Q_LENGTH=1000, MAX_CATEGORY_LENGTH=100,
MAX_BRAND_LENGTH=100, MAX_ATTR_KEY_LENGTH=100, MAX_ATTR_VALUE_LENGTH=100,
MAX_ATTR_COUNT=10 (searchParams.ts:32-37). boundedValue drops rather than
truncates (:48-50) — correct, truncation would silently change query meaning.
Attr loop breaks at the cap (:72), so the amplification vector (N attr params =>
N ANDed JSONB path conditions at productService.ts:303-305) is capped at 10.
productService.ts:335 adds take: 1000 to the previously unbounded findMany.
Enforcement is on the real request path: searchProducts has exactly one caller
(page.tsx:65), reached only after parseSearchState (page.tsx:38). No bypass.
Iteration-1 escalation (plainto_tsquery over-long lexeme, productService.ts:242-257)
resolved by construction for ASCII: 1000 chars < Postgres 2046-byte MAXSTRLEN.

Tests verified non-trivial (test13:80-120): q asserts 1000 kept / 1001 dropped /
50k dropped — a truncating impl fails it; category/brand assert both directions;
attr key/value uses strict toEqual so a leak fails. The attr-count test (:113-120)
uses toBeLessThanOrEqual(10), which would also pass if all attrs were dropped —
that degenerate case is excluded by :63, :110 and :200-232, so the fix is proven,
but the assertion alone is weak. See F5.

## Checked (all iron rules, iteration 2)
- PCI scope: no finding. No card/token/M-Pesa field in any changed file.
- Payment idempotency: N/A. No webhook, mutation, or stock decrement.
- Auth: no finding. No credential field, custom JWT, or session table.
- Presence-check middleware: N/A. No protected route added.
- Secrets: no finding. No real value, no new process.env reference, nothing logged.
- Admin audit log: N/A. No admin mutation.
- Injection/input validation: no finding. Parameterized $queryRaw; anchored linear
  attr regex; __proto__ setter is a no-op on strings; F1 length/count bounds now present.
- XSS / price bypass / soft-delete leakage / data residency: no finding (unchanged from iteration 1).

## Findings — all LOW, non-blocking
- F1-R1 (LOW) src/lib/searchParams.ts:32 — MAX_Q_LENGTH counts characters, Postgres
  counts bytes. 1000 CJK chars = one ~3000-byte lexeme, over the 2046-byte limit.
  Could not execute to confirm NOTICE-and-skip vs ERROR. Worst case is a single-request
  500 (no amplification, no data exposure), fully mitigated by F3. Fix: Buffer.byteLength
  for q, or lower to 500.
- F1-R2 (LOW, advisory) src/lib/productService.ts:226 — bound is at the edge, not the
  data layer. searchProducts still accepts unbounded filters; safe with one caller, but
  any future API route/server action calling it directly re-opens F1.
- F1-R3 (correctness, not security) src/lib/productService.ts:335,349 — take: 1000 caps
  totalCount at 1000 and totalPages at 50. Fine at current scale; misreports on a larger catalog.
- F2 (LOW, open) tests/test13-product-search.test.ts:249-253 — .resolves.not.toThrow()
  passes vacuously; does not prove invalid price bounds are dropped. Owner: test owner.
- F5 (LOW, open) tests/test13-product-search.test.ts:119 — toBeLessThanOrEqual(10) should
  be toBe(10). Bundle with F2.
- F3 (LOW, open) src/app/products/page.tsx:41-61 — no error.tsx/global-error.tsx; only
  InvalidRegionError caught. Owner: storefront-admin-engineer. Mitigates F1-R1.
- F4 (LOW, pre-existing, outside this diff) src/lib/db.ts:19 — search terms could reach
  Prisma's error logger. Informational.

## Known limits
No execution capability (read-only by mandate). Unverified by repro: F1-R1 multi-byte
lexeme behavior (reasoned, not run); the full test-suite green; the FEATURES.md <200ms benchmark.

## Routing
F1 closed — no further action for catalog-inventory-engineer on this item.
F2+F5 to test owner. F3 to storefront-admin-engineer. F1-R1/R2/R3 advisory, defer to a
subsequent item. F4 informational. No product code changed by security-reviewer.
