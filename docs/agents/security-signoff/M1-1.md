# Security review — M1-1

STATUS: CLEAR

Re-review after both prior findings (MEDIUM reset-URL/PII logging, LOW
missing forged-cookie test) were addressed. Fixes read and verified in
source, not accepted on description. Full fresh pass of all seven iron
rules against current file state.

## Checked
- PCI scope: N/A — no payment code in scope. All seven files read; no card
  data, no Stripe call path. Env Stripe/M-Pesa keys are empty or
  REPLACE_ME placeholders only.
- Payment idempotency: N/A — no webhook handler, no order/stock mutation.
- Auth: CHECKED, no finding. No hand-rolled credential field on User;
  credentials only in better-auth's Account.password
  (prisma/schema.prisma:600). Account.issuer (:589) + @@unique([issuer,
  accountId]) (:604) are builtin better-auth 1.7.x, not custom auth. No
  custom JWT or session table. src/app/profile/page.tsx:12-18 independently
  verifies server-side via auth.api.getSession() and redirects on null; no
  cookieCache configured, so that call performs real signature + DB
  validation. src/middleware.ts is documented as UX-only presence check;
  matcher is exactly ["/profile/:path*"] (:22), so no redirect loop.
  src/app/api/auth/[...auth]/route.ts is a bare toNextJsHandler(auth) — no
  bypass surface. No admin route in this diff.
- Secrets: CHECKED, no finding. PRIOR FINDING CLOSED — src/lib/auth.ts:36-40
  sendResetPassword now destructures only `{ url }` (user.email is not in
  scope, so PII cannot be logged in any environment) and logs only when
  NODE_ENV !== "production", no-opping in production. Grep confirms the only
  console.* in src/ outside seed.ts is that guarded line. All six .env.*
  files hold placeholders/empty strings only (.env.example:75,
  .env.development:33, .env.production* empty/Vercel-managed).
  process.env.BETTER_AUTH_SECRET! remains safe — better-auth fails closed on
  a missing/default secret in production.
- Admin audit log: N/A — no admin mutations in scope.
- Input validation: CHECKED, no finding. Delegated to better-auth's
  server-side Zod (z.email(), password min/max), with a single generic
  INVALID_EMAIL_OR_PASSWORD across all sign-in failure modes (no user
  enumeration). No new hand-written input path; login page is a static
  placeholder with no form.
- Data residency: CHECKED, no finding. BETTER_AUTH_URL is per-region
  (ke./et./so.); AWS_REGION is af-south-1 for Ethiopia and eu-west-2 for
  Kenya/Somalia, matching run-state.md's active decision. Auth path pins no
  region.

## Prior findings — resolution
- src/lib/auth.ts:36-40 (was MEDIUM) — RESOLVED. Production no-ops entirely;
  user email removed from the callback signature so it cannot leak in any
  environment.
- tests/test6-auth.test.ts:179-232 (was LOW) — RESOLVED. New case derives the
  real better-auth session cookie name from a live Set-Cookie header
  (:203-206) rather than hardcoding it, deletes the real session first
  (:210-215), forges a garbage value under that name (:222), and asserts the
  followed-redirect pathname is /auth/login (:229). A regression removing the
  page-level getSession() would now fail.

## Findings
None.

## Known-limits
Test suite not executed (reviewer has no Bash); the forged-cookie test was
verified by reading its assertions. Non-blocking robustness note:
tests/test6-auth.test.ts:205 takes the first Set-Cookie pair; if better-auth
ever emits a non-session cookie first, the test would forge under the wrong
name and pass via the already-covered middleware path. Filtering for the pair
whose name contains "session_token" would remove that fragility.
